const appConfig = {
    apiPrefix: '/api',
    authenticatedEntryPath: '/bookings',
    unAuthenticatedEntryPath: '/sign-up',
    tourPath: '/',
    locale: 'en',
    enableMock: false,
}

export default appConfig
